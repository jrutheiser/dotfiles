#!/usr/bin/env bash

alias gw="grunt && grunt watch"
alias serve="python -m SimpleHTTPServer"

alias q="exit"
alias c="clear"
alias e="vim --"

alias cd..="cd .."

alias gpom="git push origin master"

